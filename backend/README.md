# backend

This folder contains the Python backend for Team-H.

It provides the FastAPI-based server and agent implementations used in the project.

This README was added to satisfy Poetry's `readme` requirement so the package can be installed.
